@page "/settings"
@inject NavigationManager Navigation

<PageTitle>Settings</PageTitle>

<div class="container">
    <h1>Settings</h1>
    <!-- Uncomment and implement dark mode in the future -->
    <!--
    @* Example of a dark mode toggle:
    <button type="button" class="btn btn-primary" @onclick="ToggleDarkMode">Toggle dark mode</button>
    *@
    -->
</div>

@code {
    // Uncomment and implement dark mode logic in the future
    /*
    private bool _darkMode;
    private string _darkModeClass => _darkMode ? "dark" : "light";
    private string _textClass => _darkMode ? "text-light" : "text-dark";

    protected override void OnInitialized()
    {
        var cookies = Navigation.ToBaseRelativePath(Navigation.Uri); // Replace with cookie handling
        _darkMode = cookies.Contains("darkMode=true");
    }

    private void ToggleDarkMode()
    {
        _darkMode = !_darkMode;
        // Logic to update cookie and refresh the page
    }
    */
}
