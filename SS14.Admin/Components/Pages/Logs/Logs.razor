@page "/logs"
@using Content.Server.Database
@using Microsoft.AspNetCore.Components.QuickGrid
@using SS14.Admin.AdminLogs
@using SS14.Admin.Components.Forms


<PageTitle>Logs</PageTitle>
@*Remove ability to filter and sort by impact*@
<LogFilter Filter="@_filter" FormName="LogsPageFilterForm" OnSubmit="RefreshFilter" />
<QuickGrid @ref="Grid" ItemsProvider="@_logsProvider" Pagination="@_pagination" Class="grow">
    <PropertyColumn Property="@(log => log.RoundId)" Title="Round" Class="text-primary-1"></PropertyColumn>
    @* ReSharper disable once ConditionIsAlwaysTrueOrFalseAccordingToNullableAPIContract *@
    <PropertyColumn Property="@(log => log.Round != null ? log.Round.Server.Name : "")" Title="Server" Class="text-primary-1"></PropertyColumn>
    @*<PropertyColumn Property="@(log => log.Impact)" Title="Impact" Sortable="true" Class="text-primary-1"></PropertyColumn>*@
    <TemplateColumn Title="Impact" SortBy="@(GridSort<AdminLog>.ByAscending(log => log.Impact))">
        <span class="@context.Impact.CssColorClass() font-bold">@context.Impact</span>
    </TemplateColumn>
    <PropertyColumn Property="@(log => log.Date)" Title="Date" Sortable="true" IsDefaultSortColumn InitialSortDirection="SortDirection.Descending" Class="text-primary-1 w-48"></PropertyColumn>
    <PropertyColumn Property="@(log => log.Type)" Title="Type" Class="text-primary-1"></PropertyColumn>
    <PropertyColumn Property="@(log => log.Message)" Title="Message" Class="text-primary-1"></PropertyColumn>
</QuickGrid>
@*@if (!Grid?.Items?.Any() ?? true)
{
    <div class="grow-[100] flex justify-center items-center">
        <p>No logs found.</p>
    </div>
}*@

<Pagination OnRefreshRequired="Refresh" ShowPerPage="false" class="py-2 border-t sticky-pagination" State="@_pagination" />
